import { useState } from 'react';import { useAuth } from '../context/AuthContext';import { useNavigate } from 'react-router-dom';
export default function LoginPage(){const [email,setEmail]=useState('admin@construction.local'),[password,setPassword]=useState('Admin@123'),[err,setErr]=useState(''),[loading,setLoading]=useState(false);const {login}=useAuth(),nav=useNavigate();async function submit(e){e.preventDefault();setLoading(true);setErr('');try{await login(email,password);nav('/')}catch(e){setErr(e.response?.data?.message||'Login failed')}finally{setLoading(false)}}return <div className="login-screen"><div className="login-card"><div className="login-brand">🏗</div><h1>Construction ERP</h1><p>Project, Finance & Site Management</p>{err&&<div className="alert danger">{err}</div>}<form onSubmit={submit}><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label><button className="btn primary wide" disabled={loading}>{loading?'Signing in...':'Sign In'}</button></form><small>Demo: admin@construction.local / Admin@123</small></div></div>}
