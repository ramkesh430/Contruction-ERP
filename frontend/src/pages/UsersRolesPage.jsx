import { useEffect,useState } from 'react';import api from '../services/api';import Modal from '../components/Modal';import DataTable from '../components/DataTable';
export default function UsersRolesPage(){const [users,setUsers]=useState([]),[roles,setRoles]=useState([]),[open,setOpen]=useState(false),[permOpen,setPermOpen]=useState(false),[selectedRole,setSelectedRole]=useState(null),[permissions,setPermissions]=useState([]),[f,setF]=useState({});const load=()=>Promise.all([api.get('/users'),api.get('/roles')]).then(([u,r])=>{setUsers(u.data);setRoles(r.data)});useEffect(()=>{load()},[]);async function save(e){e.preventDefault();await api.post('/users',f);setOpen(false);setF({});load()}async function manage(role){setSelectedRole(role);const {data}=await api.get('/permissions',{params:{role_id:role.id}});setPermissions(data);setPermOpen(true)}async function savePerms(){await api.put(`/roles/${selectedRole.id}/permissions`,{permission_ids:permissions.filter(p=>p.assigned).map(p=>p.id)});setPermOpen(false)}return <><div className="page-head"><div><h1>Users / Roles / Permissions</h1><p>Create users and assign permission codes by role.</p></div><button className="btn primary" onClick={()=>setOpen(true)}>+ Add User</button></div><div className="two-col"><div className="panel"><h3>Users</h3><DataTable rows={users} columns={[{key:'name',label:'Name'},{key:'email',label:'Email'},{key:'role_name',label:'Role'},{key:'is_active',label:'Status',render:r=><span className="badge">{r.is_active?'Active':'Disabled'}</span>}]}/></div><div className="panel"><h3>Roles</h3><DataTable rows={roles} columns={[{key:'name',label:'Role'},{key:'description',label:'Description'},{key:'action',label:'Permissions',render:r=><button className="btn small" onClick={()=>manage(r)}>Manage</button>}]}/></div></div><Modal open={open} title="Add User" onClose={()=>setOpen(false)}><form className="form-grid" onSubmit={save}><label>Name<input required onChange={e=>setF({...f,name:e.target.value})}/></label><label>Email<input type="email" required onChange={e=>setF({...f,email:e.target.value})}/></label><label>Phone<input onChange={e=>setF({...f,phone:e.target.value})}/></label><label>Role<select required onChange={e=>setF({...f,role_id:e.target.value})}><option value="">Select</option>{roles.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label className="full">Password<input type="password" required minLength="8" onChange={e=>setF({...f,password:e.target.value})}/></label><div className="form-actions full"><button type="button" className="btn" onClick={()=>setOpen(false)}>Cancel</button><button className="btn primary">Create User</button></div></form></Modal><Modal open={permOpen} title={`Permissions — ${selectedRole?.name||''}`} onClose={()=>setPermOpen(false)} wide><div className="permissions-grid">{permissions.map((p,i)=><label key={p.id} className="perm"><input type="checkbox" checked={!!p.assigned} onChange={e=>setPermissions(permissions.map((x,n)=>n===i?{...x,assigned:e.target.checked}:x))}/><span>{p.code}</span></label>)}</div><div className="form-actions"><button className="btn" onClick={()=>setPermOpen(false)}>Cancel</button><button className="btn primary" onClick={savePerms}>Save Permissions</button></div></Modal></>}
