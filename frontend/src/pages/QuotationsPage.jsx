import { useEffect,useState } from 'react';import api from '../services/api';import Modal from '../components/Modal';import DataTable from '../components/DataTable';
export default function QuotationsPage(){const [rows,setRows]=useState([]),[clients,setClients]=useState([]),[open,setOpen]=useState(false),[f,setF]=useState({vat_rate:13,items:[{description:'',unit:'Sq.ft',quantity:1,rate:0}]});const load=()=>Promise.all([api.get('/quotations'),api.get('/clients')]).then(([q,c])=>{setRows(q.data);setClients(c.data)});useEffect(()=>{load()},[]);const upd=(i,k,v)=>setF({...f,items:f.items.map((x,n)=>n===i?{...x,[k]:v}:x)});async function save(e){e.preventDefault();await api.post('/quotations',f);setOpen(false);load()}return <><div className="page-head"><div><h1>Quotation & BOQ</h1><p>Create estimates with item-wise quantity, rate, VAT and total.</p></div><button className="btn primary" onClick={()=>setOpen(true)}>+ New Quotation</button></div><div className="panel"><DataTable rows={rows} columns={[{key:'quotation_no',label:'Quotation No.'},{key:'client_name',label:'Client'},{key:'quotation_date',label:'Date'},{key:'grand_total',label:'Grand Total',render:r=>'Rs. '+Number(r.grand_total||0).toLocaleString('en-IN')},{key:'status',label:'Status',render:r=><span className="badge">{r.status}</span>}]}/></div><Modal open={open} title="Create Quotation" onClose={()=>setOpen(false)} wide><form onSubmit={save}><div className="form-grid"><label>Client<select value={f.client_id||''} onChange={e=>setF({...f,client_id:e.target.value})}><option value="">Select client</option>{clients.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Title<input value={f.title||''} onChange={e=>setF({...f,title:e.target.value})}/></label><label>VAT Rate %<input type="number" value={f.vat_rate} onChange={e=>setF({...f,vat_rate:e.target.value})}/></label><label>Discount<input type="number" value={f.discount||''} onChange={e=>setF({...f,discount:e.target.value})}/></label></div><div className="line-items"><div className="line-head"><b>BOQ / Quotation Items</b><button type="button" className="btn small" onClick={()=>setF({...f,items:[...f.items,{description:'',unit:'',quantity:1,rate:0}]})}>+ Item</button></div>{f.items.map((x,i)=><div className="line-row" key={i}><input placeholder="Category" value={x.category||''} onChange={e=>upd(i,'category',e.target.value)}/><input className="desc" placeholder="Description" required value={x.description} onChange={e=>upd(i,'description',e.target.value)}/><input placeholder="Unit" value={x.unit} onChange={e=>upd(i,'unit',e.target.value)}/><input type="number" placeholder="Qty" value={x.quantity} onChange={e=>upd(i,'quantity',e.target.value)}/><input type="number" placeholder="Rate" value={x.rate} onChange={e=>upd(i,'rate',e.target.value)}/><b>Rs. {(Number(x.quantity)*Number(x.rate)).toLocaleString('en-IN')}</b></div>)}</div><div className="form-actions"><button type="button" className="btn" onClick={()=>setOpen(false)}>Cancel</button><button className="btn primary">Save Quotation</button></div></form></Modal></>}
